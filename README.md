# Attendance Summary Tool

This is a small Python project for testing a coding agent.

The project summarizes attendance records for simple class sessions. It receives a list of attendance values, calculates the attendance percentage, and decides whether the attendance level is acceptable.

The project can fail when the attendance list is empty because the percentage calculation divides by the total number of records.
